# Claude Code 초보자 안내서

## 글의 목적과 대상
이 글은 안트로픽 클로드 해커톤 우승자가 공유한 Claude Code 운영 노하우를 바탕으로, 처음 시작하는 분들이 실제로 적용할 수 있게 풀어쓴 안내서입니다. 핵심은 Skills, Hooks, Subagents, MCPs, Plugins를 “왜/언제/어떻게” 쓰는지 이해하고, 최소 구성으로 시작하는 것입니다.

---

## 준비물
- Claude Code 사용 환경(터미널 기반)
- 기본적인 파일/폴더 개념
- 30~60분의 여유

확인: Claude Code가 실행되는 터미널에서 작업 디렉터리 접근이 가능하면 준비 완료입니다.

---

## 용어 한 줄 정리
아래 개념은 서로 목적이 다릅니다. 처음에는 “대체 관계가 아니라 역할 분담”으로 이해하면 편합니다.

| 개념 | 한 줄 정의 | 어디에 저장? | 언제 쓰나? |
| --- | --- | --- | --- |
| Skills | 반복 작업을 묶은 워크플로우 규칙 | `~/.claude/skills` | 여러 단계의 작업을 한 번에 수행할 때 |
| Commands | 즉시 실행하는 짧은 프롬프트 | `~/.claude/commands` | 자주 쓰는 단일 작업을 빠르게 실행할 때 |
| Hooks | 특정 이벤트에 자동 실행되는 트리거 | 설정 JSON | 툴 실행 전/후 자동 검증이나 안내가 필요할 때 |
| Subagents | 제한된 역할을 맡는 보조 에이전트 | `~/.claude/agents` | 계획/테스트/리뷰를 분리해서 처리할 때 |
| MCPs | 외부 서비스와 연결하는 도구 | MCP 서버 설정 | DB/배포/외부 문서 접근이 필요할 때 |
| Plugins | 설치형 묶음(스킬+훅+MCP 등) | 플러그인 마켓 | 세팅 시간을 줄이고 싶을 때 |

---

## Step 1. 최소 스킬부터 시작하기
스킬은 “긴 프롬프트를 축약한 템플릿”입니다. 초보자는 먼저 한 개만 만들어보는 게 좋습니다.

예시: 리팩터링 정리용 스킬

```markdown
# ~/.claude/skills/refactor-clean.md
이 코드를 정리합니다:
- 사용되지 않는 함수/파일을 찾습니다
- 불필요한 주석과 중복 코드를 제거합니다
- 변경 이유를 요약합니다
```

검증: `/refactor-clean`를 호출했을 때, Claude가 위 지시를 “그대로” 따라가면 성공입니다.

---

## Step 2. Commands로 자주 쓰는 작업을 단축하기
Commands는 “짧고 즉각적인 실행”을 위한 용도입니다. 스킬보다 짧게 씁니다.

예시: 테스트 실행 명령

```markdown
# ~/.claude/commands/test-coverage.md
테스트 커버리지를 확인하고 요약합니다.
```

검증: `/test-coverage`가 바로 실행되는지 확인합니다.

---

## Step 3. Hooks로 자동 안전장치 만들기
Hooks는 특정 이벤트에 자동으로 작동하는 안전장치입니다. 초보자에게 가장 효율적인 활용법은 “반복 실수 방지”입니다.

예시: 긴 명령 전에 tmux 안내

```json
{
  "PreToolUse": [
    {
      "matcher": "tool == \"Bash\" && tool_input.command matches \"(npm|pnpm|yarn|cargo|pytest)\"",
      "hooks": [
        {
          "type": "command",
          "command": "if [ -z \"$TMUX\" ]; then echo '[Hook] Consider tmux for session persistence' >&2; fi"
        }
      ]
    }
  ]
}
```

검증: `pnpm` 같은 명령 실행 전에 경고가 보이면 성공입니다.

---

## Step 4. Subagents로 역할을 분리하기
Subagent는 메인 Claude의 부담을 줄여주는 “작은 담당자”입니다. 예를 들어, 코드 리뷰는 리뷰어에게 맡기고, 메인은 구현에 집중할 수 있습니다.

초보 추천 구성
- `planner.md`: 기능 계획만 담당
- `code-reviewer.md`: 품질/보안 리뷰 담당
- `tdd-guide.md`: 테스트 작성 담당

검증: 각 subagent가 맡은 역할만 수행하는지 확인하세요. 도구 권한을 제한하면 효과가 큽니다.

---

## Step 5. MCPs는 “적게, 꼭 필요한 것만”
MCP는 외부 서비스와 연결을 쉽게 해줍니다. 하지만 너무 많이 켜면 컨텍스트가 줄어듭니다.

핵심 규칙
- 설정은 많이 해도, 활성화는 적게
- 프로젝트별로 `disabledMcpServers`를 관리
- 20~30개 설정, 실제 활성 5~6개 수준이 이상적

검증: `/mcp`나 `/plugins`로 활성 상태를 확인해보세요.

---

## Step 6. Plugins는 “세팅 시간 절약용”
플러그인은 스킬+훅+MCP를 패키지로 묶은 형태입니다. 초보자에게는 설치만으로 결과가 나오는 것이 장점입니다.

자주 쓰는 플러그인 예시
- `typescript-lsp`: 타입/정의 탐색
- `hookify`: 훅을 대화로 생성
- `mgrep`: 향상된 검색

검증: 플러그인 설치 후 `/plugins`에서 활성 상태를 확인합니다.

---

## Step 7. 컨텍스트 윈도우 관리가 실력입니다
Claude Code는 컨텍스트가 생명입니다. MCP/플러그인을 과하게 켜면 200k가 70k처럼 줄어듭니다.

초보 체크리스트
- 사용하지 않는 MCP는 비활성화
- 활성 도구 수를 80 이하로 유지
- 장기 작업은 `/compact`를 고려

검증: 응답이 느려지거나 문맥이 끊기면, 가장 먼저 MCP/플러그인 활성 수를 점검하세요.

---

## 바로 적용하는 초보자 시나리오
1) 스킬 1개 만들기: `/refactor-clean`  
2) 커맨드 1개 만들기: `/test-coverage`  
3) 훅 1개 추가: tmux 안내  
4) Subagent 1개 추가: `code-reviewer.md`  
5) MCP 1~2개만 활성화: GitHub, Supabase 등

이 단계만 해도 작업 반복이 줄고 품질 검증 루틴이 생깁니다.

---

## 실전 팁 모음
- `@`로 파일 찾기, `/`로 커맨드 실행, `!`로 bash 빠른 실행
- `git worktree`로 병렬 작업, `/fork`로 대화 분기
- 긴 명령은 `tmux`로 관리하면 끊김을 줄일 수 있습니다
- 에디터(Zed, VSCode)와 병행하면 변경 추적이 쉬워집니다

---

## 마무리 정리
Claude Code는 설정을 많이 하는 도구가 아니라, 반복을 줄이는 자동화 도구입니다. 초보자라면 “스킬 1개 + 커맨드 1개 + 훅 1개”로 시작하세요. 그리고 필요할 때만 MCP/플러그인을 늘리는 방식이 가장 안정적입니다.

다음 단계로는 본인의 프로젝트에 맞는 규칙(`.rules`)과 subagent 역할 분리를 확장해보시면 좋습니다.
