name: Build and Push MCP Proxy (GHCR)

on:
  push:
    branches:
      - main
    paths:
      - 'mcp-proxy-server/**'
      - '.github/workflows/build-and-push-mcp-proxy.yml'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push-mcp-proxy:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      IMAGE_TAG: ${{ github.sha }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to GHCR using GITHUB_TOKEN
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push mcp-proxy image
        uses: docker/build-push-action@v4
        with:
          context: ./mcp-proxy-server
          file: ./mcp-proxy-server/Dockerfile
          target: production
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/my-blog-mcp-proxy:${{ env.IMAGE_TAG }}
            ghcr.io/${{ github.repository_owner }}/my-blog-mcp-proxy:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Verify image pushed
        run: |
          echo "Pushed: ghcr.io/${{ github.repository_owner }}/my-blog-mcp-proxy:${IMAGE_TAG} and :latest"
