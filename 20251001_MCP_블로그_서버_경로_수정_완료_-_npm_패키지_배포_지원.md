---
title: "MCP 블로그 서버 경로 수정 완료 - npm 패키지 배포 지원"
tags: [
  "MCP",
  "npm",
  "경로설정",
  "파일시스템",
  "개발"
]
date: 2025-10-01T14:13:02.071Z
---

# MCP 블로그 서버 파일 저장 경로 문제 해결

## 수정된 사항

MCP 블로그 서버의 파일 저장 경로 문제를 해결했습니다. 이제 npm 패키지로 배포해도 각 사용자의 환경에 맞게 작동합니다.

### 주요 변경사항

#### 1. 환경 변수 로딩 경로 수정
```typescript
// 기존 (문제)
dotenv.config({ path: path.join(__dirname, "../../.env") });

// 수정 후
dotenv.config({ path: path.join(__dirname, "../.env") });
```

컴파일된 `dist` 디렉토리에서 실행되므로 상위 디렉토리의 `.env`를 올바르게 로드합니다.

#### 2. 상대 경로 지원 추가
```typescript
// filesystem.ts 개선
if (configuredDir.startsWith('./') || !path.isAbsolute(configuredDir)) {
  const packageRoot = path.join(__dirname, '../..');
  postsDir = path.resolve(packageRoot, configuredDir);
}
```

이제 `.env` 파일에서 상대 경로를 사용할 수 있습니다:
- `BLOG_POSTS_DIR=./posts` (권장)
- `BLOG_POSTS_DIR=./blog-content`

#### 3. 기본값 개선
환경 변수가 설정되지 않은 경우 자동으로 패키지 루트의 `posts` 디렉토리를 사용합니다.

## npm 패키지 배포 지원

### 사용자별 설정 방법

npm으로 설치한 사용자는 `.env` 파일에서 간단히 상대 경로를 설정할 수 있습니다:

```bash
# .env 파일
BLOG_POSTS_DIR=./my-posts  # 패키지 설치 위치 기준
```

### 절대 경로 vs 상대 경로

- **상대 경로 (권장)**: `./posts`, `../posts`
  - npm 패키지 배포에 적합
  - 사용자 환경에 관계없이 작동
  
- **절대 경로**: `/Users/username/blog/posts`
  - 특정 시스템에만 작동
  - npm 배포 시 문제 발생 가능

## 테스트 결과

✅ `.env` 파일 정상 로드
✅ 상대 경로 `./posts` 올바르게 해석
✅ 파일이 `/mcp-blog-server-ts/posts/` 디렉토리에 저장됨
✅ npm 패키지 배포 준비 완료

## 결론

이제 MCP 블로그 서버는:
1. 올바른 위치에 마크다운 파일을 저장합니다
2. npm 패키지로 배포 가능합니다
3. 사용자별 환경에 맞게 설정 가능합니다
4. 상대 경로를 통해 이식성이 향상되었습니다

모든 사용자가 자신의 환경에 맞게 간편하게 설정할 수 있는 유연한 구조가 완성되었습니다!