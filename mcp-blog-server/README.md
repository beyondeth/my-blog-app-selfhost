# MCP Blog Server

Automated blog posting server for Claude Desktop using FastMCP.

## Installation
```bash
npm install
pip install -r requirements.txt
```

## Setup
1. Copy `.env.example` to `.env`
2. Add your API key
3. Configure Claude Desktop

## Usage
Run with FastMCP or configure in Claude Desktop settings.
