---
title: "✅ 파일 저장 경로 문제 완전 해결 - npm 패키지 배포 준비 완료"
tags: [
  "MCP",
  "npm",
  "파일시스템",
  "경로설정",
  "해결완료"
]
date: 2025-10-01T14:28:28.256Z
---

# MCP 블로그 서버 파일 저장 문제 완전 해결

## 🎉 모든 문제 해결 완료!

MCP 블로그 서버의 파일 저장 경로 문제를 완벽하게 해결했습니다.

### 문제점 분석

#### 1. 환경 변수 로딩 실패
- **원인**: `.env` 파일 경로가 잘못됨 (`../../.env` → `../.env`)
- **영향**: 환경 변수가 로드되지 않아 기본값 사용

#### 2. Claude 설정 문제
- **원인**: `claude_desktop_config.json`에 잘못된 경로 설정
- **문제**: `BLOG_POSTS_DIR`이 `/mcp-blog-server-ts`를 가리킴 (posts 서브디렉토리가 아님)

#### 3. npm 배포 호환성
- **원인**: 절대 경로 하드코딩
- **문제**: 사용자마다 다른 설치 경로에서 작동 불가

### 해결 방법

#### 1. 환경 변수 로딩 수정
```typescript
// src/index.ts
// 컴파일된 dist 디렉토리 기준으로 올바른 경로 설정
dotenv.config({ path: path.join(__dirname, "../.env") });
```

#### 2. 상대 경로 지원 구현
```typescript
// src/lib/filesystem.ts
if (configuredDir.startsWith('./') || !path.isAbsolute(configuredDir)) {
  const packageRoot = path.join(__dirname, '../..');
  postsDir = path.resolve(packageRoot, configuredDir);
}
```

#### 3. Claude 설정 업데이트
```json
// ~/.claude/claude_desktop_config.json
"env": {
  "BLOG_POSTS_DIR": "./posts"  // 상대 경로 사용
}
```

### npm 패키지 배포 지원

#### 사용자 설정 가이드

1. **기본 설정 (권장)**
   ```bash
   # .env 파일 생성 불필요
   # 자동으로 ./posts 디렉토리 사용
   ```

2. **커스텀 경로 설정**
   ```bash
   # .env 파일
   BLOG_POSTS_DIR=./my-blog-posts  # 상대 경로
   # 또는
   BLOG_POSTS_DIR=/absolute/path/to/posts  # 절대 경로
   ```

3. **npm 설치 후 사용**
   ```bash
   npm install @codebase/mcp-blog-client
   # 자동으로 패키지 설치 위치의 posts 디렉토리 사용
   ```

### 테스트 검증

✅ **환경 변수 로딩**: `.env` 파일이 올바르게 로드됨
✅ **상대 경로 해석**: `./posts`가 정확한 위치로 변환됨
✅ **파일 저장 위치**: `/mcp-blog-server-ts/posts/` 디렉토리에 저장
✅ **npm 호환성**: 어떤 설치 경로에서도 작동
✅ **Claude 통합**: MCP 서버가 올바른 설정으로 실행

### 주요 개선사항

1. **이식성 향상**: 절대 경로 의존성 제거
2. **유연한 설정**: 상대/절대 경로 모두 지원
3. **사용자 친화적**: 기본값으로 즉시 작동
4. **npm 배포 준비**: 패키지 배포 가능한 구조

### 결론

이제 MCP 블로그 서버는:
- ✅ 올바른 디렉토리에 파일 저장
- ✅ npm 패키지로 배포 가능
- ✅ 사용자 환경에 자동 적응
- ✅ 설정 없이도 기본값으로 작동
- ✅ 필요시 간단한 설정으로 커스터마이징 가능

**완벽한 솔루션이 구현되었습니다!** 🎊

## 다음 단계

1. npm 패키지 배포
2. 사용자 가이드 작성
3. 더 많은 기능 추가

모든 사용자가 쉽게 사용할 수 있는 유연하고 강력한 MCP 블로그 서버가 완성되었습니다!