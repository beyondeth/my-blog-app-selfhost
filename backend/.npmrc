# Mailer template engines are optional peers and are not used by this service.
# Keep peer auto-install disabled here; runtime sharp platform packages remain
# optional dependencies and continue to be installed.
auto-install-peers=false
